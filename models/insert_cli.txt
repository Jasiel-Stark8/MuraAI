from models.users import User
from app import app, db
with app.app_context():
    db.create_all()
    new_user = User(email="example@email.com", password_hash="somehashedpassword")
    db.session.add(new_user)
    db.session.commit()
